package models;

public class Transaction {
	
	// checks if credit card on an order is a valid card
	public static boolean isValidCreditCardAccount(Order order) {
		return true;
	}
	
	// gets the balance of the card on the order
	public static double getBalance(Order order) {
		return 0.0;
	}
	
	// sets the balance of the card on the order
	public static void updateBalance(Order order) {
		
	}
}
